{% extends 'base.html.twig' %}

{% block title %}Motel{% endblock %}

{% block body %}
    <h1>Motel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ motel.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ motel.nom }}</td>
            </tr>
            <tr>
                <th>Emplacement</th>
                <td>{{ motel.emplacement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_motel_index') }}">back to list</a>

    <a href="{{ path('app_motel_edit', {'id': motel.id}) }}">edit</a>

    {{ include('motel/_delete_form.html.twig') }}
{% endblock %}
