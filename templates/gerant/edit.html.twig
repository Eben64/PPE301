{% extends 'base.html.twig' %}

{% block title %}Gerant{% endblock %}

{% block contenu %}
    <h1>Edit Gerant</h1>

    {{ include('gerant/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_gerant_index') }}">back to list</a>

    {{ include('gerant/_delete_form.html.twig') }}
{% endblock %}
