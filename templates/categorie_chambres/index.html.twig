{% extends 'base.html.twig' %}

{% block title %}CategorieChambres index{% endblock %}

{% block contenu %}
    <h1>CategorieChambres index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomCategorieChambre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_chambre in categorie_chambres %}
            <tr>
                <td>{{ categorie_chambre.id }}</td>
                <td>{{ categorie_chambre.nomCategorieChambre }}</td>
                <td>
                    <a href="{{ path('app_categorie_chambres_show', {'id': categorie_chambre.id}) }}">show</a>
                    <a href="{{ path('app_categorie_chambres_edit', {'id': categorie_chambre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_chambres_new') }}">Create new</a>
{% endblock %}
