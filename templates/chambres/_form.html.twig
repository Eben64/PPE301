{{ form_start(form) }}
    {{ form_widget(form, {'attr': {'class': 'form form-control'}}) }}
</br>
    <div>
    <button class="btn btn-primary btn-primary-hover">{{ button_label|default('AJOUTER') }}</button>
    <a href="{{ path('app_chambres_index') }}" class="btn btn-primary">RETOUR</a>

    <a href="{{ path('app_categorie_chambres_new') }}" class="btn btn-primary" style="margin-left: 48em;">Ajouter une Catégorie de Chambres</a></br></br></br></br>
    </div>
    
{{ form_end(form) }}
